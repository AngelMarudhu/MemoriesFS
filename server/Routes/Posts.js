// import express from 'express';
// import { getPosts, createPost } from '../Controllers/Posts.js';

// // why we are using the router method because it will help us to move the routes in seperate files
// // express router actually returns the middleware thats why we using the routes in use method
// const router = express.Router();

// router.get('/', getPosts);
// router.post('/', createPost);
// //router.patch('/:id', updatePost);

// export default router;
